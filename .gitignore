# dependencies
node_modules/

# build output
dist/

# vercel
.vercel/

# local env files — never commit real keys.
# .env.example is the committed template (whitelisted below).
.env
.env.local
.env.*.local
!.env.example

# logs / os
*.log
.DS_Store
